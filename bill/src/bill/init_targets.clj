(ns bill.init-targets
  (:require bill.targets.deps
            bill.targets.install
            bill.targets.install-file
            bill.targets.install-maven))

; This file does nothing but load other files with targets.